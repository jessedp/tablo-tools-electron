// import { CHANGE_VIEW, SEND_RESULTS } from '../actions/types';
// import type { Action } from './types';
// import { ViewType, VIEW_GRID } from './constants';
// import Airing from '../utils/Airing';

// // not currently used
// export function changeView(state: ViewType = VIEW_GRID, action: Action) {
//   const { view } = action;

//   switch (action.type) {
//     case CHANGE_VIEW:
//       return view;

//     default:
//       return state;
//   }
// }
// export function sendResults(state: Array<Airing> = [], action: Action) {
//   const { results } = action;

//   switch (action.type) {
//     case SEND_RESULTS:
//       return results;

//     default:
//       return state;
//   }
// }

// /**
//       searchAlert: results.searchAlert,
//       loading: results.loading,
//       airingList: results.airingList,
//       view: results.view,

// =========
//       loading: false,
//       view,
//       airingList: actionList,
//       searchAlert,
//       actionList,

// */
